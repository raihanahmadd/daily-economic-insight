# Economic Intelligence System — Dependencies
# Install with: pip install -r requirements.txt

# Data sources
fredapi==0.5.1        # FRED API client
yfinance>=1.4.1       # Yahoo Finance market data. 1.x bundles curl_cffi and
                      # impersonates a browser internally (avoids 429 rate-limiting).
                      # Older 0.2.x breaks on newer curl_cffi: "'str' object has no
                      # attribute 'name'". Do NOT pass a custom session to yf.download.
curl_cffi>=0.15       # Pulled in by yfinance; pinned here so the impersonation backend is present.

# HTTP & environment
requests>=2.31.0      # World Bank API + FRED calendar
python-dotenv>=1.0.0  # Load .env API keys

# Time & data handling
pytz>=2024.1          # JST timezone support
pandas>=2.0.0         # Time series manipulation (required by yfinance)
